<div class="settings">
	<section class="settings__section">
		<h2 class="settings__label" translate>Change password</h2>
		<form (submit)="wSecuritySubmit(); $event.preventDefault()" class="settings__content">
			<div>
				<input pPassword class="w-full" [feedback]="false" [placeholder]="translateService.translate('Enter your current password...')()" [formField]="securityForm.currentPassword" />
				<app-field-error [field]="securityForm.currentPassword" />
			</div>
			<div>
				<input pPassword class="w-full" [feedback]="false" [placeholder]="translateService.translate('Enter your new password...')()" [formField]="securityForm.newPassword" />
				<app-field-error [field]="securityForm.newPassword" />
			</div>
			<div>
				<input pPassword class="w-full" [feedback]="false" [placeholder]="translateService.translate('Confirm your new password...')()" [formField]="securityForm.confirmPassword" />
				<app-field-error [field]="securityForm.confirmPassword" />
			</div>
			<button pButton type="submit" [disabled]="isSecurityDisabled()"><span translate>Update password</span></button>
		</form>
	</section>

	<section class="settings__section">
		<h2 class="settings__label" translate>Theme</h2>
		<div class="settings__content settings__content--inline settings__theme">
			<div class="settings__theme-row">
				<span class="settings__theme-label" translate>Mode</span>
				<div class="settings__theme-group">
					@for (m of themeService.modes(); track m) {
						<button
							pButton
							type="button"
							size="small"
							class="settings__theme-btn"
							[text]="themeService.mode() !== m"
							[class.settings__theme-btn--inactive]="themeService.mode() !== m"
							[attr.aria-pressed]="themeService.mode() === m"
							(click)="themeService.setMode(m)"
						>
							{{ m }}
						</button>
					}
				</div>
			</div>

			<div class="settings__theme-row">
				<span class="settings__theme-label" translate>Density</span>
				<div class="settings__theme-group">
					@for (d of themeService.densities(); track d) {
						<button
							pButton
							type="button"
							size="small"
							class="settings__theme-btn"
							[text]="themeService.density() !== d"
							[class.settings__theme-btn--inactive]="themeService.density() !== d"
							[attr.aria-pressed]="themeService.density() === d"
							(click)="themeService.setDensity(d)"
						>
							{{ d }}
						</button>
					}
				</div>
			</div>

			<div class="settings__theme-row">
				<span class="settings__theme-label" translate>Radius</span>
				<div class="settings__theme-group">
					@for (r of themeService.radiuses(); track r) {
						<button
							pButton
							type="button"
							size="small"
							class="settings__theme-btn"
							[text]="themeService.radius() !== r"
							[attr.aria-pressed]="themeService.radius() === r"
							(click)="themeService.setRadius(r)"
						>
							{{ r }}
						</button>
					}
				</div>
			</div>
		</div>
	</section>

	<section class="settings__section">
		<h2 class="settings__label" translate>Switch language</h2>
		<div class="settings__language-picker">
			<button
				(click)="isLanguageListOpen.set(!isLanguageListOpen())"
				class="settings__language"
				[attr.aria-label]="translateService.translate('Switch language')()"
				[attr.aria-expanded]="isLanguageListOpen()"
				type="button"
			>
				<span class="settings__flag" aria-hidden="true">{{ languageFlagEmoji(languageService.language()) }}</span>
				<span [translate]="languageService.getLanguage(languageService.language())?.name ?? ''"></span>
				<i class="pi pi-chevron-down" aria-hidden="true"></i>
			</button>
			@if (isLanguageListOpen()) {
				<ul class="settings__languages">
					@for (language of languageService.languages(); track language.code) {
						<li>
							<button
								(click)="setLanguage(language.code)"
								class="settings__language-option"
								[class.settings__language-option--active]="language.code === languageService.language()"
								type="button"
							>
								<span class="settings__flag" aria-hidden="true">{{ languageFlagEmoji(language.code) }}</span>
								<span [translate]="language.name"></span>
							</button>
						</li>
					}
				</ul>
			}
		</div>
	</section>

	<div class="settings__actions"><button pButton severity="secondary" type="button" (click)="userService.logout()"><i class="pi pi-sign-out"></i><span translate>Logout</span></button></div>
</div>
