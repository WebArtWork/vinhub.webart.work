import { companyProfile } from '../../company/company.data';

/** Default OG/Twitter image for Vinhub pages that don't have their own entity photo. */
export const vinhubSeoImage = companyProfile.image;
